import Error from '../components/Error';

const NotFound = () => {
  return (
    <div className="w-full min-h-[calc(100vh-80px)] overflow-hidden flex justify-center items-center">
      <Error />
    </div>
  );
};

export default NotFound;
